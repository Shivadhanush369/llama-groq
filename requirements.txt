fastapi==0.95.2        # FastAPI framework
uvicorn==0.22.0        # ASGI server for FastAPI
groq==0.11.0          # Groq API client (make sure this version matches the actual library you're using)
pydantic==1.10.2       # Data validation (used by FastAPI)

